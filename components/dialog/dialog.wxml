<view class="dialog " hidden="{{isShowDia}}" catchtouchmove="noscroll">
    <view class="dialog-mask" bindtap="cencleDia"></view>
    <view class="dialog-main animated zoomIn" style="width:{{width}}rpx;padding-bottom:{{padding}}rpx">
        <slot></slot>
    </view>
</view>
